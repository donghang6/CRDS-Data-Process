# 连续吸收数据处理方法（论文正文版）

## 数据预处理

连续吸收实验的原始数据以单个激光波数点为单位记录。每个波数点包含多次 cavity ring-down 事件，每次事件给出对应的衰荡时间、拟合残差、腔体温度和腔体压力。数据处理首先根据文件名读取激光波数，并按波数从低到高排序。对于相邻波数间隔明显异常的点，依据全波段相邻间隔的中位数和标准差进行识别并剔除，以避免跳点、重复点或局部异常采样影响后续拟合。

对于每个波数点的多次衰荡时间测量，采用迭代 sigma clipping 方法剔除离群事件。具体而言，在每次迭代中计算当前保留衰荡时间的平均值和标准差，并剔除偏离平均值超过 3 倍标准差的事件。该过程最多迭代 5 次，直到没有新的离群点被剔除为止。若某一波数点经离群值剔除后的有效衰荡事件数少于 5 个，则该波数点不参与后续分析。最终以保留事件的平均值作为该波数点的代表性衰荡时间，其标准差用于表征重复测量离散度。

## 腔内损耗谱计算

经预处理得到的平均衰荡时间记为 \(\tau(\tilde{\nu})\)，其中 \(\tilde{\nu}\) 为波数。根据 CRDS 中衰荡时间与腔内损耗的关系，将衰荡时间转换为单位长度的腔内总损耗：

\[
L(\tilde{\nu}) = \frac{1}{c\tau(\tilde{\nu})}.
\]

在实际数据处理中，衰荡时间以 \(\mu s\) 为单位，损耗以 ppm cm\(^{-1}\) 表示，因此采用如下换算：

\[
L_{\mathrm{ppm/cm}}(\tilde{\nu}) =
\frac{10^{12}}{c \, \tau_{\mu s}(\tilde{\nu})},
\]

其中 \(c = 2.99792458 \times 10^{10}\) cm s\(^{-1}\)。若存在参考空腔衰荡时间或参考损耗谱，则样品吸收系数可由样品总损耗与参考损耗之差得到：

\[
\alpha(\tilde{\nu}) =
L_{\mathrm{sample}}(\tilde{\nu}) -
L_{\mathrm{ref}}(\tilde{\nu}).
\]

在未引入独立参考谱的情况下，后续连续吸收背景分析直接基于腔内总损耗 \(L(\tilde{\nu})\) 进行。

## Ar 数据的连续背景拟合

Ar 数据中不包含 O2 分子窄线吸收，因此其腔内损耗谱主要表现为随波数缓慢变化的连续背景。为获得平滑的背景曲线，在损耗域对 \(L(\tilde{\nu})\) 进行滑动窗口局部多项式拟合。沿波数轴以固定步长设置一系列局部拟合中心；对于每个中心，选取其附近有限宽度的波数窗口，并在窗口内进行二阶多项式拟合。为降低局部异常点的影响，窗口内拟合采用 robust sigma clipping。每个窗口仅保留多项式在窗口中心处的拟合值作为背景锚点。

获得所有局部锚点后，采用保形分段三次 Hermite 插值（PCHIP）将锚点连接成全波段连续背景。PCHIP 插值能够在保持曲线单调性和局部形状方面优于普通三次样条，从而减少宽带连续背景中不必要的过冲。对于 Ar 500 Torr 数据，局部拟合窗口宽度为 40 cm\(^{-1}\)，锚点间隔为 5 cm\(^{-1}\)，多项式阶数为 2，robust clipping 阈值为 4 倍 robust sigma。为进一步抑制小尺度噪声，得到的背景曲线再经过 20 cm\(^{-1}\) 宽度的 Savitzky-Golay 平滑。

拟合得到的损耗背景记为 \(L_{\mathrm{fit}}(\tilde{\nu})\)。相应的等效衰荡时间由下式反算：

\[
\tau_{\mathrm{fit}}(\tilde{\nu}) =
\frac{10^{12}}{c \, L_{\mathrm{fit}}(\tilde{\nu})}.
\]

残差定义为实测损耗与拟合背景之差：

\[
\Delta L(\tilde{\nu}) =
L(\tilde{\nu}) - L_{\mathrm{fit}}(\tilde{\nu}).
\]

## O2 数据中窄线吸收的剔除

与 Ar 数据不同，O2 数据中叠加了大量 O2 分子窄线吸收。窄线吸收会增加腔内损耗并降低衰荡时间，如果直接对全部损耗点进行背景拟合，所得曲线会受到窄吸收线的系统性影响。因此，O2 数据处理中首先利用 HITRAN2024 数据库计算与实验条件相同的 O2 吸收谱，并以此识别需要从连续背景拟合中剔除的吸收区域。

HITRAN 仿真采用实验记录中的温度和压力。温度取该组数据中腔体温度的中位数，压力取腔体压力的正值中位数；若压力列不可用，则从实验压力标签中读取。仿真采用 Voigt 线型，温度和压力分别设为：

\[
T = T_{\mathrm{exp}} + 273.15,
\quad
p = \frac{p_{\mathrm{Torr}}}{760}.
\]

HITRAN 仿真波数范围覆盖实验波段并在两端各扩展 0.5 cm\(^{-1}\)，仿真步长为 0.002 cm\(^{-1}\)。仿真得到的吸收系数从 cm\(^{-1}\) 转换为 ppm cm\(^{-1}\)，并插值到实验波数点，得到 \(L_{\mathrm{HITRAN}}(\tilde{\nu})\)。

根据 HITRAN 仿真谱自动生成 O2 吸收 mask。设仿真损耗峰值为 \(L_{\mathrm{HITRAN}}^{\max}\)，当某一实验波数点满足：

\[
L_{\mathrm{HITRAN}}(\tilde{\nu})
\ge 0.01 L_{\mathrm{HITRAN}}^{\max}
\]

时，该点被标记为 O2 吸收区。为了避免线翼附近的残余吸收参与背景拟合，该 mask 在波数轴上向两侧各扩展 0.05 cm\(^{-1}\)。被 mask 标记的点不参与 O2 连续背景拟合。

## O2 CIA 基线拟合

在剔除 HITRAN 标记的 O2 窄线吸收区后，仅使用非吸收区的实测损耗点拟合缓慢变化的 CIA 基线。该拟合仍在损耗域进行，方法与 Ar 数据的局部多项式锚点拟合相同：在波数轴上设置局部窗口，对每个窗口中的非吸收点进行 robust 二阶多项式拟合，并取窗口中心处拟合值作为基线锚点；随后使用 PCHIP 插值连接所有锚点，得到连续的 O2 CIA 基线。

对于 O2 500 Torr 数据，局部拟合窗口宽度为 8 cm\(^{-1}\)，锚点间隔为 1 cm\(^{-1}\)，多项式阶数为 2，robust clipping 阈值为 2 倍 robust sigma。拟合后的基线再经过 2 cm\(^{-1}\) 宽度的 Savitzky-Golay 平滑。最终得到的 O2 CIA 基线记为：

\[
L_{\mathrm{CIA}}(\tilde{\nu}).
\]

其对应的等效衰荡时间为：

\[
\tau_{\mathrm{CIA}}(\tilde{\nu}) =
\frac{10^{12}}{c \, L_{\mathrm{CIA}}(\tilde{\nu})}.
\]

O2 数据的残差可表示为：

\[
\Delta L_{\mathrm{O2}}(\tilde{\nu}) =
L(\tilde{\nu}) - L_{\mathrm{CIA}}(\tilde{\nu}),
\]

其中残差主要包含被剔除区域附近的 O2 窄线吸收贡献、测量噪声及可能存在的未完全建模结构。

## 样条平滑后处理

在获得初步的连续背景或 CIA 基线后，对部分数据进一步采用三次 B-spline 进行平滑后处理。该步骤不作用于原始衰荡时间，而只用于处理后的基线曲线，以减小局部连接处的小折点并获得更平滑的连续曲线。

平滑曲线表示为：

\[
\tau_{\mathrm{spline}}(\tilde{\nu}) =
\sum_j c_j B_j(\tilde{\nu}),
\]

其中 \(B_j\) 为三次 B-spline 基函数，\(c_j\) 为待优化系数。系数通过最小二乘优化获得，目标函数由数据残差和可选平滑惩罚共同组成：

\[
R =
\sum_i
\left[
\tau_{\mathrm{spline}}(\tilde{\nu}_i) -
\tau_{\mathrm{fit}}(\tilde{\nu}_i)
\right]^2
+
\lambda
\sum_j
\left(
\Delta^2 c_j
\right)^2.
\]

其中 \(\lambda\) 为平滑惩罚系数，\(\Delta^2 c_j\) 为 B-spline 系数的二阶差分。该惩罚项用于抑制相邻节点之间不必要的快速振荡。Ar 数据采用全局三次 B-spline 拟合，内部节点间隔为 15 cm\(^{-1}\)，平滑惩罚系数为 0.1。O2 数据可根据不同波段的数据质量和曲率使用分区域 B-spline 拟合；分区域拟合时，每个目标区域两侧额外加入 5 cm\(^{-1}\) 的上下文宽度，以降低区域边界处的不连续风险。

若样条结果用于替换原有基线衰荡时间，则相应的损耗基线按同一 CRDS 换算关系重新计算：

\[
L_{\mathrm{spline}}(\tilde{\nu}) =
\frac{10^{12}}{c \, \tau_{\mathrm{spline}}(\tilde{\nu})}.
\]

## 等间隔波数网格重采样

为便于 Ar 和 O2 数据之间的比较以及后续作图，将最终得到的衰荡时间曲线或 CIA 基线统一插值到等间隔波数网格。统一网格范围为 9120-9820 cm\(^{-1}\)，步长为 0.01 cm\(^{-1}\)。重采样采用 PCHIP 插值，以保持宽带连续背景的局部形状并避免普通三次样条可能引入的过冲。最终输出的等间隔数据用于后续定量比较、图像绘制和论文数据整理。

## 可直接放入论文的精简版

原始 CRDS 数据以单个激光波数点为单位记录，每个波数点包含多次衰荡事件。首先根据文件名提取波数，并对所有波数点按波数排序。对相邻波数间隔异常的点进行剔除后，对每个波数点的多次衰荡时间采用迭代 3 sigma clipping 去除离群事件。每次迭代计算当前保留事件的平均值和标准差，并剔除偏离平均值超过 3 倍标准差的事件，最多迭代 5 次。保留事件数少于 5 的波数点不参与后续分析。每个波数点最终取过滤后衰荡时间的平均值作为代表性衰荡时间。

平均衰荡时间 \(\tau\) 被转换为腔内总损耗 \(L\)。当 \(\tau\) 以 \(\mu s\) 为单位、损耗以 ppm cm\(^{-1}\) 表示时，二者关系为 \(L = 10^{12}/(c\tau)\)，其中 \(c = 2.99792458 \times 10^{10}\) cm s\(^{-1}\)。若存在参考空腔损耗，则吸收系数由样品损耗减去参考损耗得到。

Ar 数据不含明显 O2 窄线吸收，因此直接在损耗域拟合缓慢变化背景。具体方法是在波数轴上设置一系列局部拟合窗口，在每个窗口内进行 robust 二阶多项式拟合，并取窗口中心处的拟合值作为背景锚点。所有锚点随后通过 PCHIP 插值连接成全波段连续背景，并可进一步用 Savitzky-Golay 方法平滑。Ar 500 Torr 数据采用 40 cm\(^{-1}\) 的局部窗口、5 cm\(^{-1}\) 的锚点间隔、4 sigma robust clipping 和 20 cm\(^{-1}\) 的平滑宽度。

O2 数据中存在大量窄线吸收，因此首先使用 HITRAN2024 数据库在实验温度和压力下计算 O2 吸收谱。仿真采用 Voigt 线型，波数步长为 0.002 cm\(^{-1}\)。当 HITRAN 仿真损耗大于其峰值的 1% 时，对应实验点被标记为 O2 吸收区，并在波数轴两侧各扩展 0.05 cm\(^{-1}\)。这些点不参与 CIA 基线拟合。剩余非吸收区点采用与 Ar 相同的局部 robust 多项式/PCHIP 方法拟合 CIA 基线。O2 500 Torr 数据采用 8 cm\(^{-1}\) 的局部窗口、1 cm\(^{-1}\) 的锚点间隔、2 sigma robust clipping 和 2 cm\(^{-1}\) 的平滑宽度。

为进一步降低局部连接处的小折点，处理后的基线衰荡时间可使用三次 B-spline 进行后处理平滑。B-spline 系数通过最小二乘优化获得，并加入二阶系数差分惩罚以抑制不必要的高频振荡。Ar 数据采用全局 B-spline，内部节点间隔为 15 cm\(^{-1}\)，平滑惩罚系数为 0.1。最终，所有处理结果均采用 PCHIP 插值到 9120-9820 cm\(^{-1}\)、间隔 0.01 cm\(^{-1}\) 的统一波数网格。
