# 原始衰荡数据直接计算吸收系数的论文表述

为检验连续吸收数据处理流程对最终吸收系数的影响，选取 303 K、500 Torr 条件下的 O2 和 Ar 原始衰荡数据进行了直接计算对比。O2 数据的实际实验条件由原始文件统计得到，温度为 303.1501 K，压力为 501.3 Torr；Ar 背景数据的压力中位数为 499.93 Torr。首先，将 O2 和 Ar 原始衰荡时间分别直接插值到 0.01 cm^-1 的统一波数网格，并由

```text
L = 1 / (c tau)
```

换算为腔损耗。随后，在相同温度和压力条件下利用 HITRAN2024 模拟 O2 线吸收。考虑到 HITRAN 线位置与实测谱线之间可能存在微小波数偏差，在扣除前对 HITRAN 线吸收谱进行了波数轴平移对齐；本组数据得到的最优平移量为 -0.009 cm^-1。直接计算得到的连续吸收系数表示为：

```text
alpha_direct(nu) = L_O2(nu) - L_HITRAN,O2(nu) - L_Ar(nu)
```

其中 `L_O2` 和 `L_Ar` 分别为由原始 O2 与 Ar 衰荡时间换算得到的总腔损耗，`L_HITRAN,O2` 为对齐后的 HITRAN O2 线吸收。该结果与经完整处理流程得到的吸收系数：

```text
alpha_processed(nu) = L_O2,baseline(nu) - L_Ar,baseline(nu)
```

进行比较，用于评估原始数据直接计算、HITRAN 线吸收扣除以及后续基线处理对连续吸收结果的影响。

