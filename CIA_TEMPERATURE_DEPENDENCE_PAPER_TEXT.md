# 论文中可用的温度依赖系数处理描述

O2-O2 碰撞诱导吸收二元系数的温度依赖性按逐波数点进行分析。首先，在每个波数处由吸收系数和 O2 数密度计算二元碰撞吸收系数：

```text
B(nu) = alpha(nu) / rho^2
```

其中 `alpha(nu)` 为扣除背景后的吸收系数，`rho` 为以 amagat 表示的 O2 数密度。`B` 的标准不确定度由吸收系数和数密度的不确定度通过误差传播得到：

```text
u_B = |B| sqrt[ (u_alpha / alpha)^2 + (2u_rho / rho)^2 ]
```

对于 303 K 下 500、600 和 700 Torr 的三组数据，先在每个波数处按反方差权重合成为一个 303 K 等效数据点：

```text
B_303 = sum_i w_i B_i / sum_i w_i,    w_i = 1/u_i^2
```

合成值的内部标准不确定度为：

```text
u_303,internal = sqrt(1 / sum_i w_i)
```

并用 303 K 三个压力点相对于加权平均值的约化卡方计算 Birge ratio：

```text
R_303 = max(1, sqrt(chi2_red,303))
```

最终 303 K 合成点的不确定度取：

```text
u_303 = R_303 u_303,internal
```

随后，对每个波数处的 273、303 和 333 K 三个温度点进行加权线性拟合：

```text
B(nu,T) = a(nu) + b(nu)T
```

其中斜率：

```text
b(nu) = dB(nu)/dT
```

定义为该波数处的温度依赖系数。拟合采用权重 `1/u_B^2`，参数由加权最小二乘求得：

```text
beta = (X^T W X)^(-1) X^T W y
```

其中 `beta = [a, b]^T`。斜率的内部标准不确定度由协方差矩阵给出：

```text
Cov(beta) = (X^T W X)^(-1)
```

即：

```text
u_internal(dB/dT) = sqrt[Cov(beta)_{11}]
```

为避免温度点之间的离散程度导致不确定度被低估，进一步根据温度线性拟合残差计算约化卡方和 Birge ratio：

```text
chi2_fit = sum_j [B_j - (a + bT_j)]^2 / u_j^2
R_fit = max(1, sqrt(chi2_fit / dof))
```

其中自由度 `dof = N - 2`。最终温度依赖系数的标准不确定度为：

```text
u(dB/dT) = R_fit u_internal(dB/dT)
```

在图中，左图选取若干代表性波数点展示 `B` 随温度的线性变化，误差棒表示 `B` 的标准不确定度；右图给出全波段的 `dB/dT`，阴影表示 `±u(dB/dT)`。为了进行保守估计，也对所有输入 `u_B` 统一放大两倍后重复相同的加权合成和线性拟合流程；该结果用于评估输入 `B` 不确定度可能偏小对最终温度依赖系数不确定度的影响。

## 可直接放入论文的方法段落

O2-O2 碰撞诱导吸收二元系数的温度依赖性在每个波数点处独立确定。首先由扣除背景后的吸收系数和 O2 数密度计算二元碰撞吸收系数 `B(nu)=alpha(nu)/rho^2`，并通过误差传播得到其标准不确定度。303 K 下测得的 500、600 和 700 Torr 三组结果先按反方差权重 `w_i=1/u_i^2` 合成为一个等效的 303 K 数据点，其不确定度取加权平均的内部不确定度并根据同温度不同压力点的约化卡方用 Birge ratio 进行修正。随后，对每个波数点处 273、303 和 333 K 的 `B` 值进行加权线性最小二乘拟合，模型为 `B(nu,T)=a(nu)+b(nu)T`，其中斜率 `b(nu)` 即温度依赖系数 `dB(nu)/dT`。拟合权重取 `1/u_B^2`，斜率的内部标准不确定度由协方差矩阵 `(X^T W X)^(-1)` 得到。为避免实验点离散程度超过输入不确定度时低估斜率不确定度，最终 `u(dB/dT)` 进一步乘以温度线性拟合的 Birge ratio `max(1,sqrt(chi^2_red))`。因此，图中 `dB/dT` 的不确定度同时反映了各温度下 `B` 的传播不确定度、303 K 重复压力组的一致性以及温度线性拟合残差。

